# ── Required ──────────────────────────────────────────────────────────────────

# Supabase REST API (used by daily pipeline + GitHub Actions)
SUPABASE_URL=https://uqfmzlomufjlujzpfnrn.supabase.co
SUPABASE_KEY=your-supabase-anon-or-service-role-key

# Direct Postgres connection (used by --backfill mode for fast bulk inserts)
# Find at: Supabase Dashboard > Project Settings > Database > Connection string > Session mode
DATABASE_URL=postgresql://postgres.uqfmzlomufjlujzpfnrn:YOUR-PASSWORD@aws-0-eu-west-2.pooler.supabase.com:5432/postgres

# ── Optional (tuning) ────────────────────────────────────────────────────────

# API_CONCURRENCY=20       # Max concurrent ERG API requests
# DB_CONCURRENCY=5         # Max concurrent Supabase REST writes (keep <=5)
# TASK_CONCURRENCY=30      # Max sensor x pollutant tasks in flight
# INSERT_BATCH_SIZE=200    # Rows per REST API insert batch
# PG_CONCURRENCY=20        # Max concurrent Postgres connections (backfill mode)
# PG_BATCH_SIZE=1000       # Rows per Postgres insert batch (backfill mode)
